#include <iostream>
#include<bits/stdc++.h>
using namespace std;

int main()
{
    int a[] = {2,7,10,10,10,20,20};
    int cnt = 1;
    int target = 20;
    int ind = lower_bound(a,a+7,target)-a;
    int me = upper_bound(a,a+7,target)-a;
    int diff = me-1;
   cout<<(diff-ind)+1;
    
}
